version: '3.8'

services:
  gallery:
    build: .
    container_name: image-gallery
    ports:
      - "7860:7860"
    volumes:
      - ./images:/app/images
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=7860
    restart: unless-stopped